import 'package:flutter/material.dart';

const tuneRounded = Icon(Icons.tune_rounded);
const closeIcon = Icon(Icons.close);
const arrowBack = Icons.arrow_back;
const searchIcon = Icon(Icons.search);
const menuBook = Icons.menu_book;
const visibilityOutlined = Icons.visibility_outlined;
const personPinRounded = Icons.person_pin_rounded;
